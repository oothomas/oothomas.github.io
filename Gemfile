source "https://rubygems.org"

# Pin to the GitHub Pages stack (locks Jekyll, jekyll-remote-theme, jekyll-sass-converter, etc.)
gem "github-pages", group: :jekyll_plugins

# Needed for local serve on Ruby 3.x
gem "webrick", "~> 1.8"
